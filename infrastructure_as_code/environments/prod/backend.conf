# backend.conf — Production Environment
# Pass to every stack at init time:
# terraform init -backend-config="../backend.conf" -backend-config="key=int_<module>.tfstate"

resource_group_name  = "rg-tfstate-iflow-prod"
storage_account_name = "stotfstateiflowprod"
container_name       = "tfstate"
use_azuread_auth     = true
# key is set per-stack via: -backend-config="key=int_<module>.tfstate"
